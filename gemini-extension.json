{
  "name": "chime",
  "version": "0.1.2",
  "mcpServers": {
    "nodeServer": {
      "command": "node",
      "args": ["${extensionPath}${/}dist${/}chime.js"],
      "cwd": "${extensionPath}"
    }
  },
  "contextFileName": "GEMINI.md",
  "description": "Get notified by your Gemini CLI. Enable Gemini CLI to play precise audible notifications that signal task completion, user confirmation prompts, and long-running operation finishes.",
  "excludeTools": ["run_shell_command"]
}
